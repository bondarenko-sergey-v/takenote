Run the client locally:

- Install dependencies with `npm install --cache /tmp/takenote-npm-cache`.
- Start the client with `npm run client`.
- Open `http://127.0.0.1:2990` in your browser, then stop the client with `Ctrl+C` in the terminal.
