# Run Client

Install dependencies:

```bash
npm install --cache /tmp/takenote-npm-cache
```

Start the client:

```bash
npm run client
```

Open `http://127.0.0.1:2990`.

Stop the client:

```bash
Ctrl+C
```
